{
  "name": "cosmos-eto-my",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173",
    "build": "vite build",
    "test": "vitest run --exclude tests/**/*.e2e.spec.js",
    "test:coverage": "vitest run --coverage --exclude tests/**/*.e2e.spec.js",
    "test:e2e": "playwright test",
    "preview": "vite preview --host 127.0.0.1 --port 5173",
    "serve": "node serve-dist.js",
    "assets:optimize": "node scripts/optimize-assets.mjs"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "three": "^0.178.0",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.61.1",
    "@vitest/coverage-v8": "^4.1.10",
    "jsdom": "^29.1.1",
    "sharp": "^0.35.3",
    "vitest": "^4.1.10"
  }
}
